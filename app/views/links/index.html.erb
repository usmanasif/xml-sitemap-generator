<!-- <div class="col-xs-3 pull-right">
  <%= link_to 'Add Link','javascript:;', class: "btn btn-success btn-block add_var pull-right" %>
</div>
<table class="table table-stripped">
  <thead>
    <tr>
      <th>Host</th>
      <th>Actions</th>
    </tr>
  </thead>
  <tbody>
    <% @links.all.try(:each) do |link| %>
      <tr>
        <td>
          <%= link.host %>
        </td>
        <td>
          <%= link_to "Generate Sitemap",link_generate_sitemap_path(link_id: link.id), remote: true,:class => "btn btn-outline-default edit_var" %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<div class="modal fade" id="add-new-link" role="document">
  <div class="modal-dialog">
    <div class="modal-content">
      <%= form_for @new_link, remote: true do |f| %>
        <div class="modal-header">
          <h4 class="modal-title">Add Link</h4>
        </div>
        <div class="modal-body">
          <div class="field2 m-b">
            <%= label_tag :host,"Host",class: "col-xs-3" %>
            <%= f.text_field :host,class: "form-control col-xs-9 var_name",required: true %>
          </div>
        </div>
        <div class="modal-footer">
          <%= submit_tag "ADD",class: "btn btn-success col-xs-4 add_variable" %>
        </div>
      <% end %>
    </div>
  </div>
</div>


 -->
